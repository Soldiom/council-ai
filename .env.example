# ============================================================================
# LLM Provider Configuration
# ============================================================================
# Choose your provider:
# - hf_inference (RECOMMENDED: Free cloud API, no downloads!)
# - mock (local testing, no API)
# - huggingface (local models, requires download)
# - openai, anthropic, azure (paid APIs)

DEFAULT_PROVIDER=hf_inference

# ============================================================================
# FREE Cloud Models - Hugging Face Inference API (RECOMMENDED)
# ============================================================================
# Get a FREE token at: https://huggingface.co/settings/tokens
# 
# TOKEN ACCESS REQUIRED: "Read" (this is the default when creating a token)
# - Click "New token"
# - Name: council-agent (or any name)
# - Access: Read (default) ✓
# - Click "Generate a token"
# - Copy the token (starts with hf_)
# 
# See HF_TOKEN_SETUP.md for detailed instructions
#
# No downloads, no GPU needed - models run on HF's servers for free!

HF_API_TOKEN=hf_your_read_token_here

# Recommended free cloud models (no download needed):
HF_INFERENCE_MODEL=mistralai/Mistral-7B-Instruct-v0.2
# HF_INFERENCE_MODEL=meta-llama/Llama-2-7b-chat-hf
# HF_INFERENCE_MODEL=HuggingFaceH4/zephyr-7b-beta
# HF_INFERENCE_MODEL=tiiuae/falcon-7b-instruct
# HF_INFERENCE_MODEL=microsoft/phi-2

# ============================================================================
# FREE Local Models - Hugging Face Transformers (Optional)
# ============================================================================
# Use these if you want to run models locally (requires download)
# To use: Set DEFAULT_PROVIDER=huggingface

HF_MODEL=microsoft/phi-2
# HF_MODEL=gpt2
# HF_MODEL=TinyLlama/TinyLlama-1.1B-Chat-v1.0

# Device: -1 for CPU, 0 for GPU (cuda:0), 1 for cuda:1, etc.
HF_DEVICE=-1

# ============================================================================
# Paid API Providers (require API keys)
# ============================================================================
# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Anthropic
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Azure OpenAI
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ============================================================================
# Optional Tools
# ============================================================================
# Tavily (web search)
TAVILY_API_KEY=tvly-your-tavily-api-key-here

# ============================================================================
# Model Settings
# ============================================================================
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=2000